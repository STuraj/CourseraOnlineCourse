package module3;

public class Tsk1 {
    public static void main(String[] args) {
        int count = 0;
        while (count < 3) {
            count += 1;
            Object newDna = newDna + dna.substring(startPos, pos);
            startPos = pos+1;
            pos = dna.indexOf("T", startPos);
            if (pos == -1) {
                break;
            }
        }
    }
}
